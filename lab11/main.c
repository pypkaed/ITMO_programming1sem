/* #include <stdio.h>
#include "Zagolovochny file/rhomb.h"

int main(int argv, char ** argc) {
    diamond diamond;
    printf("Insert the coordinates of rhomb's vertex: \n");
    printf("A:");
    scanf("%lf %lf", &diamond.x1, &diamond.y1);
    printf("B:");
    scanf("%lf %lf", &diamond.x2, &diamond.y2);
    printf("C:");
    scanf("%lf %lf", &diamond.x3, &diamond.y3);
    printf("D:");
    scanf("%lf %lf", &diamond.x4, &diamond.y4);
    side(&diamond);
    double diamond_per = perimeter(&diamond);
    double diamond_area = area(&diamond);

    printf("Perimeter of the rhomb is %lf\n", diamond_per);
    printf("Area of the rhomb is %lf\n", diamond_area);

    return 0;
}
*/